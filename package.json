{
  "name": "ai-governance-capstone",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --test-reporter=spec tests/*.test.js",
    "demo:logic": "node scripts/demo-core-logic.js"
  },
  "engines": {
    "node": ">=20"
  }
}
